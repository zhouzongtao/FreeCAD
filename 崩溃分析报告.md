# FreeCAD OsgVerse 崩溃分析报告

## 🔍 崩溃信息

**异常代码**: 0xC0000005 (访问违例 - Access Violation)
**异常标志**: 0

## 📊 关键发现

### 1. 程序实际上启动成功了！

从崩溃日志可以看到，程序加载了大量模块：
- ✅ FreeCADGui.dll 已加载
- ✅ FreeCADApp.dll 已加载
- ✅ FreeCADBase.dll 已加载
- ✅ 所有 Qt 模块已加载
- ✅ OSG DLL **没有**出现在日志中（说明没有加载 OSG）
- ✅ Coin4.dll 已加载（说明使用的是 Coin3D）
- ✅ 多个工作台模块已加载：
  - Measure.pyd
  - Part.pyd
  - Material/Materials.pyd
  - Start/StartGui.pyd
  - Drawing/DrawingGui.pyd

### 2. 崩溃发生在关闭 Start 页面时

从堆栈跟踪可以看到：
```
StartGui::StartView::changeEvent
QMdiSubWindow::closeEvent
QTabBarPrivate::closeTab
QAbstractButton::clicked
```

**说明**: 用户点击了关闭 Start 页面的按钮，导致崩溃。

### 3. **关键发现：OsgVerse 根本没有被使用！**

从 DLL 加载列表中，我们看到：
- ✅ Coin4.dll 已加载
- ❌ 没有任何 osg*.dll 或 ot21-OpenThreads.dll

**结论**: 虽然我们设置了 OsgVerse 为默认后端，但程序实际上使用的是 Coin3D！

## 🤔 为什么 OsgVerse 没有被使用？

可能的原因：

### 原因 1: RenderManager 没有被初始化
如果 RenderManager 的初始化失败或被跳过，默认后端设置就不会生效。

### 原因 2: 延迟加载
OsgVerse 可能是延迟加载的，只有在真正需要 3D 渲染时才会加载。

### 原因 3: 回退机制
可能有某种回退机制，如果 OsgVerse 初始化失败，自动回退到 Coin3D。

## 🎯 真正的问题

**崩溃与 OsgVerse 无关！** 

崩溃发生在：
1. 程序已经启动
2. 使用 Coin3D 后端
3. 用户关闭 Start 页面时

这是一个 **Start 模块的 bug**，与 OsgVerse 完全无关！

## 📝 下一步行动

### 选项 1: 验证 OsgVerse 是否真的被设置为默认

检查 RenderManager 的初始化日志：
- 应该看到 "OsgVerseEngine: Constructor START"
- 应该看到 "RenderManager Python bindings initialized"

### 选项 2: 强制使用 OsgVerse

需要确保：
1. RenderManager 正确初始化
2. OsgVerse 引擎被注册
3. 默认后端设置生效

### 选项 3: 忽略 Start 页面崩溃

这个崩溃与 OsgVerse 无关，可以：
1. 禁用 Start 页面
2. 或者修复 Start 页面的 bug
3. 继续测试 OsgVerse

## 💡 重要发现

**FreeCAD 可以启动！** 

问题不是"无法启动"，而是：
1. OsgVerse 没有被使用（仍然使用 Coin3D）
2. Start 页面关闭时有 bug

## 🔧 建议的测试方法

### 测试 1: 禁用 Start 页面

修改 FreeCAD 配置，禁用 Start 页面，然后：
1. 启动 FreeCAD
2. 创建新文档
3. 打开 3D 视图
4. 查看是否加载 OSG DLL

### 测试 2: 检查日志

查看控制台输出，确认：
- OsgVerseEngine 是否被构造
- RenderManager 是否初始化
- 默认后端是什么

### 测试 3: 手动切换后端

如果 Python 绑定工作，尝试：
```python
import FreeCADGui
FreeCADGui.switchRenderBackend(2)  # 切换到 OsgVerse
```

## 📊 总结

1. **好消息**: FreeCAD 可以启动，所有模块都正常加载
2. **问题**: OsgVerse 没有被使用，仍然使用 Coin3D
3. **崩溃**: 与 OsgVerse 无关，是 Start 页面的 bug
4. **下一步**: 需要确认为什么 OsgVerse 没有被使用

---

**状态**: 需要进一步调查为什么 OsgVerse 没有被使用
