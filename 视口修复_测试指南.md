# 视口修复测试指南

## 修复内容

已应用 **方案 2：devicePixelRatio 修复**

### 修改的文件
- `src/Mod/OsgVerseGui/OsgVerseWidget.cpp`

### 修改内容

在 `initializeGL()` 和 `resizeGL()` 中添加了 devicePixelRatio 支持：

```cpp
// 获取实际像素大小（考虑高 DPI）
qreal dpr = devicePixelRatio();
int pixelWidth = width * dpr;
int pixelHeight = height * dpr;

// 使用实际像素大小设置视口
_graphicsWindow->resized(0, 0, pixelWidth, pixelHeight);
camera->setViewport(0, 0, pixelWidth, pixelHeight);
```

## 测试步骤

### 1. 启动 FreeCAD

```cmd
cd E:\Repository\FreeCAD\FreeCAD\build\bin
FreeCAD.exe
```

### 2. 运行测试脚本（可选）

在 FreeCAD Python 控制台中：

```python
exec(open(r'E:\Repository\FreeCAD\FreeCAD\test_viewport_fix.py', encoding='utf-8').read())
```

或者直接创建新文档测试。

### 3. 检查视口

打开 FreeCAD 后，检查以下内容：

#### ✅ 应该看到的正确现象：

1. **背景填充完整**
   - 深蓝灰色背景应该填充整个视图窗口
   - 没有黑色区域

2. **球体完整显示**
   - 绿色测试球体应该完整显示
   - 球体没有被裁剪

3. **鼠标交互正常**
   - 拖动鼠标可以旋转视图
   - 滚轮可以缩放
   - 渲染区域始终填充整个窗口

4. **窗口调整大小**
   - 调整 FreeCAD 窗口大小
   - 视口应该自动调整
   - 渲染区域始终正确

#### ❌ 如果仍然有问题：

1. **黑色区域仍然存在**
   - 说明 devicePixelRatio 方案不够
   - 需要尝试方案 3（framebuffer 绑定）

2. **球体仍然被裁剪**
   - 可能是视口坐标系问题
   - 需要检查 OSG 和 Qt 的坐标系统

## 诊断信息

### 查看控制台输出

在 Report View 中应该看到类似的日志：

```
OsgVerseWidget::initializeGL called, widget size: 400 x 300
OsgVerseWidget: Device pixel ratio: 1.00, pixel size: 400 x 300
OsgVerseWidget: GraphicsWindow resized to 400 x 300
OsgVerseWidget: Camera viewport set to 400 x 300, aspect: 1.33

OsgVerseWidget::resizeGL called: 1373 x 543
OsgVerseWidget: Viewport updated to 1373 x 543 (DPR: 1.00)
```

### 关键信息：

1. **Device pixel ratio**：应该是 1.0（普通显示器）或 1.5/2.0（高 DPI）
2. **Pixel size**：应该是 widget size × DPR
3. **Viewport size**：应该等于 pixel size

## 如果问题仍然存在

### 方案 3：Framebuffer 绑定

如果 devicePixelRatio 方案不够，需要在 `paintGL()` 中添加 framebuffer 绑定：

```cpp
void OsgVerseWidget::paintGL()
{
    if (_viewer.valid()) {
        // 绑定 Qt 的 framebuffer
        GLuint fbo = defaultFramebufferObject();
        osg::Camera* camera = _viewer->getCamera();
        
        // 设置渲染目标
        camera->setRenderTargetImplementation(
            osg::Camera::FRAME_BUFFER_OBJECT, fbo
        );
        
        _viewer->frame();
    }
}
```

### 方案 4：检查 GraphicsWindowEmbedded 配置

可能需要在创建 GraphicsWindowEmbedded 时设置正确的 traits：

```cpp
traits->x = 0;
traits->y = 0;
traits->width = width() * devicePixelRatio();
traits->height = height() * devicePixelRatio();
traits->windowDecoration = false;
traits->doubleBuffer = true;
traits->sharedContext = nullptr;
traits->pbuffer = false;  // 确保不是 pbuffer
```

## 预期结果

如果修复成功，应该看到：

1. ✅ 深蓝灰色背景填充整个窗口
2. ✅ 绿色球体完整显示在中心
3. ✅ 没有黑色区域
4. ✅ 鼠标交互流畅
5. ✅ 窗口调整大小时视口正确更新

## 下一步工作

一旦视口问题解决：

1. **移除测试球体**
   - 从 `OsgVerseViewer.cpp` 构造函数中移除测试代码

2. **实现 ViewProvider 自动添加**
   - 监听文档对象添加事件
   - 自动调用 `viewer->addViewProvider()`

3. **测试实际几何体**
   - 创建 Part::Box
   - 验证渲染正确

---

**请测试并报告结果！** 🎯
