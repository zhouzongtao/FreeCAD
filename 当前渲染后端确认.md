# 当前渲染后端确认

## 问题

用户询问：目前的渲染采用的是 OsgVerse 吗？

## 答案

**否，目前 FreeCAD 使用的是 Coin3D 渲染后端，而不是 OsgVerse。**

## 详细说明

### 1. 默认后端配置

在 `src/Gui/Render/Core/RenderEngine.h` 中，`RenderEngineFactory` 的默认后端设置为：

```cpp
class RenderEngineFactory {
    // ...
private:
    // 默认引擎类型 / Default engine type
    BackendType _defaultType{BackendType::Coin3D};  // ← 默认是 Coin3D
};
```

### 2. 初始化流程

当 FreeCAD 启动时，`RenderManager` 会调用：

```cpp
// src/Gui/Core/RenderManager.cpp
bool RenderManager::initialize()
{
    // ...
    // 创建默认引擎 / Create default engine
    _currentEngine = factory.createDefault();  // ← 创建 Coin3D 引擎
    
    if (_currentEngine) {
        _currentBackend = _currentEngine->getType();  // ← BackendType::Coin3D
        // ...
    }
}
```

### 3. Python API 状态

测试结果显示：

```python
import FreeCADGui as Gui
print('Has API:', hasattr(Gui, 'getCurrentRenderBackend'))
# 输出: Has API: False
```

**Python API 尚未暴露**，这意味着：
- `RenderManagerPy.cpp` 还未添加到构建系统
- 无法通过 Python 查询或切换后端
- 无法通过 Python 切换到 OsgVerse

### 4. OsgVerse 状态

虽然 OsgVerse 后端已经：
- ✅ 编译成功
- ✅ 可以加载（FreeCADGui.dll 不再崩溃）
- ✅ 已注册到 RenderEngineFactory

但是：
- ❌ 不是默认后端
- ❌ 没有 Python API 来切换
- ❌ 没有 GUI 界面来选择
- ❌ GraphicsWindow 未实现（无法实际渲染）

## 如何切换到 OsgVerse？

### 方法 1: 修改默认后端（C++ 代码）

修改 `src/Gui/Render/Core/RenderEngine.h`:

```cpp
class RenderEngineFactory {
    // ...
private:
    // 默认引擎类型 / Default engine type
    BackendType _defaultType{BackendType::OsgVerse};  // ← 改为 OsgVerse
};
```

然后重新编译。

### 方法 2: 添加 Python API（推荐）

1. **添加 RenderManagerPy.cpp 到构建系统**

   修改 `src/Gui/CMakeLists.txt`，添加：
   ```cmake
   set(FreeCADGui_SRCS
       # ... 其他文件
       Core/RenderManagerPy.cpp  # ← 添加这一行
   )
   ```

2. **在 Application.cpp 中注册 Python 模块**

   修改 `src/Gui/Application.cpp`，添加：
   ```cpp
   #include "Core/RenderManagerPy.h"
   
   void Application::initTypes()
   {
       // ... 其他初始化
       
       // 注册 RenderManager Python 接口
       Base::Interpreter().addMethod(
           {
               {"getCurrentRenderBackend", (PyCFunction)RenderManagerPy::getCurrentRenderBackend, METH_NOARGS, "..."},
               {"switchRenderBackend", (PyCFunction)RenderManagerPy::switchRenderBackend, METH_VARARGS, "..."},
               // ... 其他方法
               {NULL, NULL, 0, NULL}
           }
       );
   }
   ```

3. **重新编译并测试**

   ```bash
   cmake --build build --config Release --target FreeCADGui
   ```

4. **使用 Python 切换**

   ```python
   import FreeCADGui as Gui
   
   # 列出可用后端
   print(Gui.listRenderBackends())  # [1, 2]
   
   # 切换到 OsgVerse
   Gui.switchRenderBackend(2)
   
   # 确认当前后端
   print(Gui.getCurrentRenderBackend())  # 2
   ```

### 方法 3: 添加配置文件支持

创建配置文件 `user.cfg`:
```ini
[Render]
DefaultBackend=OsgVerse
```

在 `RenderManager::initialize()` 中读取配置。

### 方法 4: 添加 GUI 选项

在 FreeCAD 的首选项对话框中添加渲染后端选择：
- Tools → Edit parameters → BaseApp → Preferences → View
- 添加 "RenderBackend" 选项

## 当前状态总结

| 项目 | Coin3D | OsgVerse |
|------|--------|----------|
| 编译状态 | ✅ 成功 | ✅ 成功 |
| 可以加载 | ✅ 是 | ✅ 是 |
| 默认后端 | ✅ 是 | ❌ 否 |
| Python API | ❌ 未暴露 | ❌ 未暴露 |
| 实际渲染 | ✅ 正常 | ❌ 未实现 |
| GraphicsWindow | ✅ 完整 | ❌ 被注释 |

## 结论

**当前 FreeCAD 使用的是 Coin3D 渲染后端。**

要使用 OsgVerse，需要：
1. 添加 Python API（RenderManagerPy.cpp）
2. 实现 GraphicsWindow 集成
3. 通过 API 或配置切换到 OsgVerse

## 下一步行动

### 立即可做
1. ✅ 添加 RenderManagerPy.cpp 到 CMakeLists.txt
2. ✅ 注册 Python 方法到 FreeCADGui 模块
3. ✅ 重新编译测试

### 短期目标
1. 实现 GraphicsWindow（osgQt 或自定义）
2. 测试 OsgVerse 渲染功能
3. 添加 GUI 配置选项

### 长期目标
1. 完善 OsgVerse 功能
2. 性能优化
3. 文档和示例

---

**日期**: 2026-01-19  
**当前后端**: Coin3D (默认)  
**OsgVerse 状态**: 已编译，未启用
