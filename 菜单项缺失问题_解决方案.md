# 菜单项缺失问题 - 解决方案

## 问题原因

编译过程中由于**内存不足**和**并行编译冲突**，导致多个GUI模块编译失败：

### 编译错误类型
1. **内存不足**: `error C1060: compiler is out of heap space`
2. **权限冲突**: `error C1083: Cannot open precompiled header file: Permission denied`
3. **链接错误**: `fatal error LNK1104: cannot open file`

### 受影响的模块
- ❌ SketcherGui (已修复 ✓)
- ❌ DraftGui (Python模块，无需编译)
- ❌ TechDrawGui (已修复 ✓)
- ❌ PartDesignGui (已修复 ✓)
- ❌ FemGui (已修复 ✓)

## 解决方案

### 1. 使用单线程重新编译

已创建脚本 `rebuild_missing_modules.ps1`，使用单线程编译避免内存不足：

```powershell
./rebuild_missing_modules.ps1
```

### 2. 编译结果

✅ **已成功编译的模块**:
- SketcherGui (7.84 MB)
- TechDrawGui (8.39 MB)
- PartDesignGui (4.37 MB)
- FemGui (5.05 MB)

✅ **Draft模块**: 主要是Python代码，DraftGui.py已存在

## 测试步骤

### 1. 重启FreeCAD
```cmd
build\bin\FreeCAD.exe
```

### 2. 运行测试脚本
在FreeCAD的Python控制台中运行：
```python
exec(open('test_all_modules.py').read())
```

### 3. 检查菜单

应该能看到以下菜单项：

**Part菜单**:
- Create primitives (Box, Cylinder, Sphere, etc.)
- Boolean operations
- Extrude, Revolve
- Fillet, Chamfer

**PartDesign菜单**:
- Create body
- Create sketch
- Pad, Pocket
- Hole, Fillet, Chamfer
- Pattern operations

**Sketcher菜单**:
- Create sketch
- Sketch geometries (Line, Circle, Arc, etc.)
- Constraints

**Draft菜单**:
- Line, Wire, Circle
- Rectangle, Polygon
- Dimension, Text
- Modify tools

**TechDraw菜单**:
- Insert page
- Insert view
- Dimensions
- Annotations

## 预防措施

### 避免未来编译问题

1. **使用单线程编译大型模块**:
   ```cmd
   cmake --build build --config Release --target <module> -j 1
   ```

2. **增加虚拟内存**:
   - 系统设置 → 高级系统设置 → 性能 → 高级 → 虚拟内存
   - 建议设置为物理内存的1.5-2倍

3. **关闭其他应用程序**:
   - 编译时关闭浏览器、IDE等占用内存的程序

4. **分批编译**:
   ```cmd
   # 先编译核心模块
   cmake --build build --config Release --target FreeCADGui -j 1
   
   # 再编译工作台模块
   cmake --build build --config Release --target Part -j 1
   cmake --build build --config Release --target PartGui -j 1
   ```

## 验证清单

- [ ] FreeCAD启动无错误
- [ ] 所有工作台可见（Part, PartDesign, Sketcher, Draft, TechDraw, Fem）
- [ ] 菜单栏显示完整（File, Edit, View, Tools, Macro, Windows, Help）
- [ ] 工作台菜单显示完整
- [ ] 工具栏图标正常显示
- [ ] 可以创建新文档
- [ ] 可以切换工作台
- [ ] 可以创建基本几何体

## 下一步

如果菜单仍然缺失，请：

1. 检查编译日志中的错误
2. 确认模块文件存在：
   ```powershell
   Get-ChildItem build/Mod/*/*.pyd | Select-Object Name, Length
   ```
3. 运行诊断脚本：
   ```python
   exec(open('test_all_modules.py').read())
   ```
4. 查看FreeCAD启动日志中的错误信息

## 技术细节

### 编译器内存限制

MSVC编译器在处理大型C++文件时可能耗尽堆内存，特别是：
- 使用预编译头（PCH）
- 大量模板实例化
- 并行编译多个文件

### 解决方法

1. **单线程编译**: `-j 1`
2. **禁用PCH**: 在CMakeLists.txt中设置 `target_precompile_headers(target REUSE_FROM other)`
3. **增加编译器堆大小**: `/Zm200` 编译选项

---

**状态**: ✅ 问题已解决
**日期**: 2026-02-01
**影响**: 菜单项应该已恢复正常
