# 修复OsgVerse光照 - 说明

## 🎯 问题

OsgVerse视图中的光照不正常：
- 物体侧面太暗
- 只有顶面有明显光照
- 整体对比度过高

## 🔍 原因

之前只有一个headlight（跟随相机的方向光），导致：
- 只有面向相机的面被照亮
- 侧面和背面几乎没有光照
- 缺少环境光和填充光

## ✅ 解决方案

添加了三光源系统：

### Light 0: 主光源（Headlight）
- **类型**: 方向光，跟随相机
- **用途**: 主要照明
- **参数**:
  - Ambient: 0.4（增加环境光）
  - Diffuse: 0.8（略微降低漫反射）
  - Specular: 0.3（高光）

### Light 1: 填充光（Fill Light）
- **类型**: 方向光，固定位置
- **方向**: 右上方 (1, 1, 1)
- **用途**: 填充阴影区域
- **参数**:
  - Diffuse: 0.5（中等强度）
  - Specular: 0.2（轻微高光）

### Light 2: 背光（Back Light）
- **类型**: 方向光，固定位置
- **方向**: 左下后方 (-1, -1, -0.5)
- **用途**: 照亮背面和底部
- **参数**:
  - Diffuse: 0.3（较弱强度）
  - Specular: 0.1（很轻的高光）

### 全局设置
- **全局环境光**: 0.3（增加）
- **双面光照**: 启用
- **法线归一化**: 启用

## 🔧 编译步骤

1. **关闭FreeCAD**（重要！）

2. **重新编译OsgVerseGui**:
   ```cmd
   cmake --build build --config Release --target OsgVerseGui -j 1
   ```
   ✅ **已完成** - OsgVerseGui.pyd 编译成功

3. **重新启动FreeCAD**:
   ```cmd
   build\bin\FreeCAD.exe
   ```

4. **运行光照测试**:
   ```python
   exec(open('test_lighting_fix.py').read())
   ```

5. **手动测试光照**:
   - 切换到Part工作台
   - 创建一个Box或其他几何体
   - 旋转视图查看各个面的光照

## 📊 预期效果

修复后应该看到：
- ✅ 所有面都有适当的光照
- ✅ 侧面不再过暗
- ✅ 整体对比度更柔和
- ✅ 物体的立体感更好
- ✅ 阴影过渡更自然

## 🎨 光照对比

### 修复前：
- 顶面：很亮
- 侧面：很暗（几乎黑色）
- 底面：完全黑色

### 修复后：
- 顶面：明亮（主光源）
- 侧面：适中（填充光）
- 底面：可见（背光）

## 🔍 技术细节

### 三点光照系统
这是标准的三点光照技术：
1. **Key Light**（主光源）- 最亮，定义主要形状
2. **Fill Light**（填充光）- 中等亮度，填充阴影
3. **Back Light**（背光）- 较弱，分离物体与背景

### 光源类型选择
- 使用**方向光**而不是点光源
- 方向光不受距离影响，性能更好
- 适合CAD场景的均匀照明需求

### 参考坐标系
- Light 0: RELATIVE_RF（跟随相机）
- Light 1, 2: ABSOLUTE_RF（固定世界坐标）

## ⚙️ 可选调整

如果需要调整光照强度，可以修改以下参数：

### 增加整体亮度
```cpp
lightModel->setAmbientIntensity(osg::Vec4(0.4f, 0.4f, 0.4f, 1.0f));  // 从0.3增加到0.4
```

### 增加主光源强度
```cpp
light0->setDiffuse(osg::Vec4(1.0f, 1.0f, 1.0f, 1.0f));  // 从0.8增加到1.0
```

### 增加填充光强度
```cpp
light1->setDiffuse(osg::Vec4(0.6f, 0.6f, 0.6f, 1.0f));  // 从0.5增加到0.6
```

## 📝 与Coin3D对比

Coin3D默认也使用类似的多光源系统：
- Headlight（主光源）
- 环境光
- 可选的额外光源

现在OsgVerse的光照应该与Coin3D更接近了。

## 🎯 下一步

编译完成后：
1. 重启FreeCAD
2. 创建测试几何体
3. 旋转视图检查光照
4. 如果需要，可以进一步调整光照参数

---

**修改文件**: `src/Mod/OsgVerseGui/OsgVerseViewer.cpp`
**影响**: OsgVerse渲染后端的所有3D视图
**兼容性**: 不影响Coin3D后端
